package dev.lukamadness.madnesscore.common.slots.gui;

/**
 * Par (x, y) simple para posiciones de layout en la GUI del inventario.
 * Portado de dev.emi.trinkets.Point.
 */
public record Point(int x, int y) {
}
